name: Spring GIFT CI/CD with GitHub Actions

on:
  push:
    branches: [ "step3" ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Set up JDK 21
        uses: actions/setup-java@v3
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Grant execute permission for gradlew
        run: chmod +x ./gradlew

      - name: Build with Gradle
        run: ./gradlew build

      - name: Transfer JAR to Server
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          source: "build/libs/*.jar"
          target: "/home/ubuntu/build/libs"

      - name: Deploy to Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            export JWT_SECRET=${{ secrets.JWT_SECRET }}
            export JWT_TOKEN_TIMEOUT_SEC=${{ secrets.JWT_TOKEN_TIMEOUT_SEC }}
            export KAKAO_CLIENT_ID=${{ secrets.KAKAO_CLIENT_ID }}
            export KAKAO_CLIENT_SECRET=${{ secrets.KAKAO_CLIENT_SECRET }}
            export KAKAO_REDIRECT_URI=${{ secrets.KAKAO_REDIRECT_URI }}
            export KAKAO_AUTH_URI=${{ secrets.KAKAO_AUTH_URI }}
            export KAKAO_USER_API_URI=${{ secrets.KAKAO_USER_API_URI }}
            export SPRING_FRONT_DOMAIN=${{ secrets.SPRING_FRONT_DOMAIN }}
            cd /home/ubuntu
            ./deploy.sh